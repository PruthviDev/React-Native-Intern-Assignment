/**
 * Theme barrel – colors, typography, spacing, radius, shadows.
 */

export { lightColors, darkColors } from './colors';
export type { AppColors } from './colors';
export { typography } from './typography';
export { spacing, radius, cardShadow, elevatedShadow } from './spacing';
